[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --strict-markers
# 저장소 루트를 sys.path 에 얹는다. tests/conftest.py 도 같은 일을 하지만,
# conftest 가 임포트되기 전에 수집 단계에서 필요한 경우가 있다.
pythonpath = .
