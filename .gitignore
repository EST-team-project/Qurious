# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
venv/
.venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environment
.env
.env.dev
.env.local

# Data (large CSV files)
data/09*/
data/10*/
data/12*/
data/1*/
data/app.db
data/vectors-cache.json
# 수집기 산출물 — 원자료는 커밋하지 않는다 (SQLite·매니페스트·토큰 캐시)
data/collector/
# HF 내보내기 파케이 — 271MB 다. 저장소는 Public 이라 절대 커밋하지 않는다.
#   ⚠️ `git check-ignore -v` 로 판정하지 말 것 — 슬래시를 붙이면 빈 줄에 가짜 매치가 나
#      exit 0 을 준다. 판정은 `git status --short` 로 한다.
data/hf_export/


# OS
.DS_Store
.Identifier
Thumbs.db

# 도구가 만드는 작업 폴더 (MCP 서버·에이전트 캐시)
.playwright-mcp/
.serena/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Misc
.codex
.clude
*.pem

*.key
# KIS 접근토큰 캐시 (발급 간격 제한이 있어 재사용한다 — 절대 커밋 금지)
.kis_token_mock.json
.kis_token_real.json

# 강사님 배포 양식·교재 (교재·강의자료 커밋 금지 규칙)
# 우리가 만든 계획서는 아래 예외로 통과시킨다 — 정본은 .md 이고 .docx 는
# scripts/build_plan_docx.py 가 만드는 제출본이다.
docs/계획서/*.docx
docs/계획서/*.doc
docs/계획서/*.hwp
docs/계획서/*.pptx
docs/계획서/*.pdf
!docs/계획서/프로젝트계획서_v*.docx
