# #44 fix: [#43](../pulls/043.md) 을 main 으로 잇는다 — 머지 순서가 어긋나 orders 확장이 main 에 빠졌다

> 🗂️ 옛 GitHub PR을 옮긴 **기록 문서**입니다 — [색인](../README.md). 원문은 그대로 두고, 링크·커밋 해시만 새 자리 기준으로 바꿨습니다.

| 항목 | 내용 |
|---|---|
| 종류 | PR |
| 상태 | 머지됨 · 2026-09-20 12:33 |
| 원 작성 | 이동원(`devlee328288`) · 2026-09-20 12:33 KST |
| 브랜치 | `feat/trading-cost` → `main` |
| 머지 커밋 | [`298ae90`](https://github.com/EST-team-project/Qurious/commit/298ae90aa0b3163aafbd1af6d9316902e5421e06) |
| 바뀐 내용 | [비교 보기](https://github.com/EST-team-project/Qurious/compare/1e8607490e3cc7ca989398d06c0e3681597c9f66...474c911d6e7f44a4645d8b21a0f9a2d72ea57e73) |
| 옛 주소 | `github.com/devlee328288/Qurious/pull/44` (지금은 열리지 않음) |

## 본문

## 왜 이 PR 이 필요한가 — 머지 순서가 한 칸 어긋났다

S34 에서 연 PR 3건은 **사슬 구조**였다.

```
#41  fix/kis-tr-id        ──▶ main            (독립)
#42  feat/trading-cost    ──▶ main
#43  feat/order-cost-schema ──▶ feat/trading-cost   ← #42 보다 먼저 머지됐어야 한다
```

실제 머지 시각(KST 2026-09-20)은 이렇게 됐다.

| 순서 | PR | 방향 | 머지 시각 |
|---|---|---|---|
| 1 | [#41](../pulls/041.md) | `fix/kis-tr-id` → `main` | 12:30:01 |
| 2 | **[#42](../pulls/042.md)** | `feat/trading-cost` → **`main`** | 12:30:11 |
| 3 | **[#43](../pulls/043.md)** | `feat/order-cost-schema` → **`feat/trading-cost`** | 12:30:21 |

[#42](../pulls/042.md) 가 main 으로 들어간 **뒤에** [#43](../pulls/043.md) 이 `feat/trading-cost` 로 들어갔다. 그래서
[#43](../pulls/043.md) 의 내용은 `feat/trading-cost` 에만 있고 **`main` 에는 없다.**

```
main            32c44a6 ─ 6aa54d8(#41) ─ afc1fff ─ 88e00b3 ─ 7384a74 ─ 1e86074   ← 여기까지
                                                      │
feat/trading-cost ────────────────────────────────────┴─ 2e3cccc ─ 474c911(#43)
                                                          ↑ main 에 없는 2커밋
```

확인:

```
$ git merge-base --is-ancestor 2e3cccc main
없음
$ git log --oneline main..origin/feat/trading-cost
474c911 Merge pull request #43 from devlee328288/feat/order-cost-schema
2e3cccc feat: 주문이 정산금액으로 현금을 옮긴다 — orders 확장 + order_fills 신설
```

---

## 이 PR 이 옮기는 것 — [#43](../pulls/043.md) 그대로다

새 코드는 없다. `feat/trading-cost` 에 남아 있는 **[#43](../pulls/043.md) 의 커밋 2개를 main 으로 옮기기만 한다.**

- `orders` 테이블 11칸 → **23칸** (수수료·세금·유관기관비·정산금액·`cost_basis` 등)
- **`order_fills`** 테이블 신설 (한 주문의 부분체결을 행으로 남긴다)
- 자동매매 `int(cash // price)` 가 수수료를 빼먹어 **잔고를 넘기는 주문**을 내던 버그 수정
- `build("none")` 이 `None` 을 돌려주어 무비용을 고를 수 없던 문제 → `NoCostModel`

검증은 [#43](../pulls/043.md) 에서 이미 마쳤다 — 임시 Postgres 16(`pgvector/pgvector:pg16`)으로 왕복,
`alembic check` 에서 `orders`·`order_fills` **차이 0건**.

---

## 왜 되돌리지 않고 이어 붙이는가

`main` 은 이미 공개 이력이다. `reset`·force push 로 순서를 고치면 두 원격(GitHub·GitLab)이
갈라진다. **앞으로 이어 붙이는 것이 유일하게 안전한 방법**이라 그렇게 한다.

## 뒤집을 조건

이 PR 이 머지되면 `feat/trading-cost` 와 `main` 의 내용이 같아진다 → 그때 두 작업 브랜치
(`feat/trading-cost`·`feat/order-cost-schema`)는 삭제해도 된다.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
