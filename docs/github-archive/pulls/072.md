# #72 docs: RTM v1.0 — 개수 매듭을 계층 ID 로 풀고, 사각지대 8개를 찾았다

> 🗂️ 옛 GitHub PR을 옮긴 **기록 문서**입니다 — [색인](../README.md). 원문은 그대로 두고, 링크·커밋 해시만 새 자리 기준으로 바꿨습니다.

| 항목 | 내용 |
|---|---|
| 종류 | PR |
| 상태 | 머지됨 · 2026-09-21 15:52 |
| 원 작성 | 이동원(`devlee328288`) · 2026-09-21 15:49 KST |
| 브랜치 | `docs/rtm-v1-0` → `chore/remove-ci-pending-discussion` |
| 머지 커밋 | [`cc944e4`](https://github.com/EST-team-project/Qurious/commit/cc944e4616e8e4a2865b6f357377834bc577ae32) |
| 바뀐 내용 | [비교 보기](https://github.com/EST-team-project/Qurious/compare/2498069c6dbf477f06bbba41f5930f673bea8936...06034223325c28a51666532e6f7c19cd16f2c8c5) |
| 옛 주소 | `github.com/devlee328288/Qurious/pull/72` (지금은 열리지 않음) |

## 본문

## 무엇을 하나

**요구사항 추적표(RTM) v1.0** 과 그 **실측 스캐너**를 냅니다.
산출물 10구분 중 「요구사항 관리」의 가장 시급한 칸이었습니다.

| | 전 | 후 |
|---|---|---|
| RTM | 🔴 없음 | 🟢 `docs/요구사항/RTM_v1.0.md` |
| 요구 개수 | ⚠️ 27 vs 29 미해결 | ✅ **계층 ID 로 해소** (합계는 29) |
| 요구 원천 | 불명확 | A계열 19 + B계열 29 로 정리 |
| 실측 근거 | 없음 | `scripts/rtm_scan.py` (다시 돌릴 수 있음) |
| 요구사항 관리 채움 | 30% | 60% |

## 핵심 셋

**1. 개수 매듭은 세어서 풀리지 않았습니다.**
흔들릴 수 있었던 건 합계가 애매해서가 아니라 요구를 **순번(1…29)** 으로 부르려 했기 때문입니다.
계층 ID(`P01-③-2`)로 두면 합계가 27이든 29든 주소가 안 바뀝니다. [#62](../issues/062.md) §3.0 의 🔴 우려는 해소됩니다.

**2. 🔴 사각지대 8개를 찾았습니다.**
`rfp-2` §3.1.3 자산배분 4개 + §3.1.4 스크리닝 4개가 **목표기능표 어디에도 대응이 없습니다.**
합집합으로 보면 29가 아니라 **37**이고, 8개가 전부 **P-C 한 파트**에 몰립니다(부하 3 → 11).

**3. ⚠️ 화면이 실제보다 더 말합니다.**
`app.html:1690` 은 "AI가 최적 자산배분 비율을 계산합니다"라고 하는데,
그 화면이 부르는 `ml.py:95` 는 **위험성향 3택 하드코딩 표**입니다.
`markowitz`·`cvxpy`·`PyPortfolioOpt` 는 저장소 전체에 **0줄**입니다.
(🟢 다만 같은 엔드포인트의 종목 스코어링(샤프+Ridge)은 실재하므로 싸잡지 않았습니다.)

## 실측

```
  B계열 29개 · 판정 분포
  🟢 코드+시험   3개 (10%)   🟡 코드 있음  17개   🟠 코드 희소  4개   🔴 흔적 없음  5개
```

**시험이 붙은 요구가 29개 중 3개뿐입니다.**

## 표를 손으로 쓰지 않았습니다

손으로 쓴 RTM 은 코드가 바뀌어도 남아 한 달 뒤 거짓이 됩니다.

```bash
python scripts/rtm_scan.py          # 사람이 읽는 표
python scripts/rtm_scan.py --md     # RTM §5 에 붙일 마크다운
python scripts/rtm_scan.py --json   # 기계용
```

### ⚠️ 만들면서 두 번 속았습니다 — 둘 다 초록불 쪽으로

| # | 증상 | 원인 | 고침 |
|---|------|------|------|
| 1 | 「리밸런싱 0건」이 **1건**으로 | **스캐너 자신**이 훑는 대상에. 패턴 문자열 `r"rebalanc"` 가 잡힘 | 스캐너 파일 제외 |
| 2 | 「중복주문 방지」가 **2건** 있는 것으로 | 둘 다 **주석 속 `idempotent`**. 구현은 0건 | 코드줄·주석줄 분리 |

경위는 RTM §7.1 에 남겼습니다. **자동 판정은 편한 쪽으로 틀리기 쉽습니다.**

## 변경 파일

| 파일 | 변경 |
|------|------|
| `docs/요구사항/RTM_v1.0.md` | **신규** — 개수 확정 근거(§2) · 원천 두 계열(§3) · **사각지대 8개**(§4) · 추적표 29+8행(§5) · 대응표(§6) · **한계와 거짓 양성 기록**(§7) · 결정 항목(§9) |
| `scripts/rtm_scan.py` | **신규** — 실측 스캐너 |
| `docs/산출물목록.md` | RTM 🔴 → 🟢 · 요구사항 관리 30% → 60% · 우선순위 1위를 ERD 로 |
| `docs/TIL/이동원/2026-09-21-화면이-실제보다-더-말하고-있었다.md` | **신규** |

## 결정이 필요한 것 (RTM §9)

| # | 항목 | 누가 |
|---|------|------|
| 1 | P02 요구가 13개인가 15개인가 | 강사님께 문의 (🟡 번호는 이미 안전) |
| 2 | 사각지대 8개를 요구로 인정할 것인가 | 사용자·팀 |
| 3 | 자산배분 화면 문구를 어떻게 할 것인가 | 사용자·팀 |

## ★ 머지 순서 — 이 PR 은 스택입니다

`docs/산출물목록.md` 가 PR [#70](../pulls/070.md) 과 겹쳐 그 위에 쌓았습니다.

```
  1) 먼저  PR #70 (CI 제거)  →  main 에 머지
  2) 그 다음 이 PR 의 base 를 chore/remove-ci-pending-discussion → main 으로 바꾸고 머지
```

> ⚠️ **2번을 빼먹으면 이 PR 의 내용이 main 에 들어가지 않습니다.**
> 산출물목록 8.1절에 같은 사고가 두 번 기록돼 있습니다([#44](../pulls/044.md) · [#58](../pulls/058.md)).
> GitHub 은 base 브랜치가 이미 머지됐어도 그 브랜치로의 머지를 막지 않습니다.

Closes [#71](../issues/071.md)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
